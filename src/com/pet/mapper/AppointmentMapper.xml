<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC '-//mybatis.org//DTD Mapper 3.0/EN'
'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace="com.pet.mapper.AppointmentMapper">
	<insert id="insertApp" parameterType="aliasApp">
		insert into appointment values
		(null,#{appDate},#{appTime},#{ownerId})
	</insert>
</mapper>