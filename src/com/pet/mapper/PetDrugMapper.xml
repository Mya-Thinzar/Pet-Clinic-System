<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC '-//mybatis.org//DTD Mapper 3.0/EN'
'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace="com.pet.mapper.PetDrugMapper">
	<insert id="insertPetDrug" parameterType="aliasPetDrug">
		insert into pet_drug values
		(#{id},#{drugDate},#{drugNextDate},#{drugDesc},#{petId},#{drugId},#{doctorId})
	</insert>
	

</mapper>